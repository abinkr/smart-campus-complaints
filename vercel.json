{
  "installCommand": "cd frontend && npm ci",
  "buildCommand": "cd frontend && if [ \"$VERCEL_PROJECT_NAME\" = \"smart-campus-admin\" ]; then npm run build:admin; else npm run build:student; fi",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
